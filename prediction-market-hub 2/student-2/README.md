# Christopher Kumarelil Cherian — Portfolio & Position Tracker

Owns one frontend (HTMX), one backend/API (Flask), one database (SQLite).
Implement CRUD + an AI endpoint that calls the shared AI-Mode service.

**Reference implementation:** `student-1/` is a complete, tested example —
copy its structure (schema/seed, backend CRUD, HTMX frontend, tests).

Release 0 checklist:
- [ ] 3+ tables, 10+ rows each (database/)
- [ ] Create/Read/Update/Delete endpoints (backend/app.py)
- [ ] One /ai/* endpoint calling AI-Mode
- [ ] HTMX frontend linked from the shared home page
- [ ] pytest in tests/
- [ ] CI workflow .github/workflows/student-2.yml
